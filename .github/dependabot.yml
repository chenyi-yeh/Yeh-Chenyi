version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "job"
    schedule:
      interval: "monthly"
name: Post welcome comment
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write
jobs:
  build:
    name: Post welcome comment
    runs-on: ubuntu-latest